# Leave unset for local dev -- the Vite proxy handles /api (see vite.config.js).
# Set only for a deployed build, to the backend's full origin, no trailing slash:
# VITE_API_BASE=https://label-verifier-api.onrender.com
